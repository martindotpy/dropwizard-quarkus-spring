server:
  applicationConnectors:
    - type: http
      port: 0
  adminConnectors:
    - type: http
      port: 0

logging:
  level: INFO

mongo:
  uri: mongodb://localhost:27017/notes-db
  database: notes-db
